---
output:
  html_document: default
  pdf_document: default
  output: 
  bookdown::pdf_document2:
    highlight: zenburn
    latex_engine: xelatex
    fig_caption: yes
    number_sections: false
    toc: false
    includes:
      in_header: "paramble.tex"
    keep_tex: yes
fontsize: 11pt
header-includes:
  
geometry: margin=1in
---
# Parameter estimates for the EBS pollock reference model 
Includes parameter set from Model 20, uses the VAST index and age-composition
results  (from bottom trawl survey) and includes the Northern Bering Sea segment.
\fontsize{7}{9}
```{r eval = TRUE, include=TRUE,echo=FALSE, comment=NA }
#opts_chunk$set(fig.width = 12, fig.height = 8, echo = FALSE, warning = FALSE, message = FALSE)
ts <- readLines("../runs/07/pm.std")
for (i in 1:length(ts)) cat(ts[i], "\n")
```

# Main data file
\fontsize{3}{4}
```{r eval = TRUE, include=TRUE,echo=FALSE, comment=NA }
ts <- readLines("../runs/dat/pm_21.07.dat")
for (i in 1:length(ts)) cat(ts[i], "\n")
```
